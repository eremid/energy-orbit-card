---
name: HACS Validation

on:
  push:
  pull_request:
  schedule:
  - cron: "0 0 * * *"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  validate-hacs:
    runs-on: "ubuntu-latest"
    steps:
    - name: 🚚 Checkout
      uses: actions/checkout@v6

    - name: 🛠 HACS Validation
      uses: "hacs/action@main"
      with:
        category: "plugin"
